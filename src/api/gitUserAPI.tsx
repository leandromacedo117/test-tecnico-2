/*import {dataUser} from '../types/dataUser.ts'

export default async function GitUserAPI(userName:string):Promise<dataUser>{
   try{
    const responder = await fetch(`http://api.github.com/users/${userName}`,)
    const data = await responder.json()
    if()
    
    return data
   }
}
*/
